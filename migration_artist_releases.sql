-- ==========================================
-- МИГРАЦИЯ: ПРИВЯЗКА РЕЛИЗОВ К АРТИСТАМ
-- ==========================================

-- Добавляем колонку artist_id в таблицу релизов для связи с профилем загрузившего
ALTER TABLE public.releases ADD COLUMN IF NOT EXISTS artist_id UUID REFERENCES public.profiles(id) ON DELETE SET NULL;

-- Обновляем существующие релизы (если нужно, но пока оставляем NULL для системных)
