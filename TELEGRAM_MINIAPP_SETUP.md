# Подключение LISNETAPP к Telegram Mini App

## 📋 Шаг 1: Подготовка приложения

### 1.1 Сборка production-версии
```bash
npm run build
```

Это создаст папку `dist/` с готовым приложением.

### 1.2 Проверка локально
```bash
npm run preview
```

Откройте приложение и убедитесь, что всё работает корректно.

---

## 🌐 Шаг 2: Деплой приложения

Вам нужно разместить приложение на хостинге с HTTPS. Варианты:

### Вариант A: Vercel (Рекомендуется - бесплатно)
1. Установите Vercel CLI:
   ```bash
   npm i -g vercel
   ```

2. Деплой:
   ```bash
   vercel
   ```

3. Следуйте инструкциям в терминале
4. Получите URL вида: `https://your-app.vercel.app`

### Вариант B: Netlify (Альтернатива)
1. Установите Netlify CLI:
   ```bash
   npm i -g netlify-cli
   ```

2. Деплой:
   ```bash
   netlify deploy --prod
   ```

### Вариант C: GitHub Pages
1. Добавьте в `vite.config.ts`:
   ```ts
   export default defineConfig({
     base: '/LISNETAPP/',
     // ... остальная конфигурация
   })
   ```

2. Установите gh-pages:
   ```bash
   npm install --save-dev gh-pages
   ```

3. Добавьте в `package.json`:
   ```json
   "scripts": {
     "deploy": "npm run build && gh-pages -d dist"
   }
   ```

4. Деплой:
   ```bash
   npm run deploy
   ```

---

## 🤖 Шаг 3: Создание Telegram Mini App

### 3.1 Создайте бота (если еще нет)
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (имя и username бота)
4. Сохраните **токен бота** (понадобится для backend)

### 3.2 Настройте Mini App
1. В [@BotFather](https://t.me/BotFather) отправьте `/newapp`
2. Выберите вашего бота
3. **Title**: `LISNET Music`
4. **Description**: `Discover and share music`
5. **Photo**: Загрузите иконку приложения (512x512 px)
6. **Demo GIF/Video**: (опционально) Загрузите демо
7. **Web App URL**: Вставьте URL вашего деплоя (например, `https://your-app.vercel.app`)
8. **Short name**: `lisnet` (будет использоваться в ссылках)

### 3.3 Получите ссылку на Mini App
После настройки вы получите ссылку вида:
```
https://t.me/your_bot_name/lisnet
```

---

## 🔧 Шаг 4: Интеграция с Telegram SDK

Ваше приложение уже использует `@telegram-apps/sdk`, но давайте убедимся, что всё настроено правильно.

### 4.1 Проверьте инициализацию в `main.tsx`

Убедитесь, что Telegram WebApp инициализируется:

```tsx
import { retrieveLaunchParams } from '@telegram-apps/sdk';

// В начале main.tsx
try {
  const { initDataRaw, initData } = retrieveLaunchParams();
  console.log('Telegram Mini App initialized:', initData);
} catch (error) {
  console.log('Running outside Telegram');
}
```

### 4.2 Настройте тему Telegram

В вашем приложении уже есть поддержка темы. Убедитесь, что она синхронизируется с темой Telegram:

```tsx
import { themeParams } from '@telegram-apps/sdk';

// Применить цвета темы Telegram
document.documentElement.style.setProperty('--tg-theme-bg-color', themeParams.bgColor);
document.documentElement.style.setProperty('--tg-theme-text-color', themeParams.textColor);
```

---

## 🧪 Шаг 5: Тестирование

### 5.1 Локальное тестирование через ngrok
Если хотите протестировать локально перед деплоем:

1. Установите ngrok:
   ```bash
   brew install ngrok
   ```

2. Запустите приложение:
   ```bash
   npm run dev
   ```

3. В новом терминале:
   ```bash
   ngrok http 5173
   ```

4. Используйте HTTPS URL от ngrok в настройках Mini App

### 5.2 Тестирование в Telegram
1. Откройте ссылку Mini App в Telegram
2. Проверьте:
   - ✅ Авторизация через Telegram работает
   - ✅ Аватар пользователя отображается
   - ✅ Данные профиля загружаются
   - ✅ Все функции работают корректно

---

## 🔐 Шаг 6: Настройка Supabase для Telegram Auth

### 6.1 Обновите политики безопасности

В вашем Supabase проекте убедитесь, что RLS политики разрешают доступ для Telegram пользователей:

```sql
-- Разрешить создание профиля для новых Telegram пользователей
CREATE POLICY "Allow Telegram users to create profile"
ON profiles FOR INSERT
WITH CHECK (auth.uid() = id);

-- Разрешить чтение своего профиля
CREATE POLICY "Users can read own profile"
ON profiles FOR SELECT
USING (auth.uid() = id);
```

### 6.2 Настройте Telegram Bot Token в Supabase

1. Перейдите в Supabase Dashboard → Authentication → Providers
2. Включите "Custom" provider
3. Добавьте логику верификации Telegram initData

---

## 📱 Шаг 7: Дополнительные настройки Mini App

### 7.1 Настройте кнопки в боте
В [@BotFather](https://t.me/BotFather):

```
/setmenubutton
Выберите бота
Введите текст кнопки: "🎵 Open App"
Введите URL: https://t.me/your_bot_name/lisnet
```

### 7.2 Настройте команды бота
```
/setcommands
Выберите бота
Введите:
start - Launch LISNET Music
help - Get help
profile - View your profile
```

---

## ✅ Чек-лист готовности

- [ ] Приложение собрано (`npm run build`)
- [ ] Приложение задеплоено на HTTPS хостинге
- [ ] Бот создан в @BotFather
- [ ] Mini App настроен с правильным URL
- [ ] Telegram SDK инициализируется корректно
- [ ] Supabase настроен для Telegram авторизации
- [ ] Приложение протестировано в Telegram
- [ ] Аватар пользователя отображается
- [ ] Все функции работают

---

## 🐛 Troubleshooting

### Проблема: "Mini App не открывается"
- Проверьте, что URL использует HTTPS
- Убедитесь, что приложение доступно по URL
- Проверьте консоль браузера на ошибки

### Проблема: "Данные пользователя не загружаются"
- Проверьте инициализацию Telegram SDK
- Убедитесь, что `initData` передаётся корректно
- Проверьте настройки CORS в Supabase

### Проблема: "Аватар не отображается"
- Убедитесь, что `avatar_url` сохраняется в профиле
- Проверьте, что Telegram передаёт фото пользователя
- Проверьте политики Storage в Supabase

---

## 📚 Полезные ссылки

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Apps SDK](https://docs.telegram-mini-apps.com/)
- [Supabase Documentation](https://supabase.com/docs)
- [Vercel Deployment Guide](https://vercel.com/docs)

---

## 🚀 Следующие шаги

После успешного запуска Mini App:

1. **Добавьте аналитику** (Telegram Analytics, Google Analytics)
2. **Настройте уведомления** через Telegram Bot API
3. **Добавьте платежи** через Telegram Payments
4. **Оптимизируйте производительность** для мобильных устройств
5. **Соберите обратную связь** от первых пользователей

---

**Удачи с запуском! 🎵**
